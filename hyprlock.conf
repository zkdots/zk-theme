$color       = rgba(28,28,30,1.0)   # Dark graphite background (matches your dark panels)
$inner_color = rgba(44,44,46,0.8)   # Slightly lighter overlay for subtle depth
$outer_color = rgba(153,153,153,1.0) # Silver/grey border for lock screen frame
$font_color  = rgba(242,242,242,1.0) # Light gray text for readability
$check_color = rgba(200,200,200,1.0) # Soft silver/gray for password dots / indicators
